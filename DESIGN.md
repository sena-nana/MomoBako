# MomoBako 视觉设计标准

## 0. 适用范围

本文定义 MomoBako 的视觉语言,用于指导桌面端界面、资源仓库工作流、文档截图、后续功能界面和 UI 改动评审。

MomoBako 是面向文件资源管理的仓库工作台。它不是素材展示站、通用文件预览器、聊天应用或营销型产品。界面应帮助用户理解仓库上下文、目录结构、文件状态、元数据、历史记录、搜索结果和可执行操作。

## 1. 设计原则

### 1.1 产品气质

- 克制、清晰、可扫描。
- 信息密度适合长期整理和维护资源,但不制造噪音。
- 默认保持安静,只在冲突、失败、危险操作或需要用户确认时提高可见度。
- 能呈现同步、扫描、索引和 Revision 过程,但不让过程信息抢走资源主线。

### 1.2 视觉优先级

1. 主内容:当前仓库、目录、文件列表、预览、元数据编辑、主操作。
2. 当前状态:选中项、文件状态、筛选条件、同步状态、缓存状态、服务连接状态。
3. 过程信息:扫描结果、文件系统事件、Revision、索引进度、缓存命中、插件处理记录。
4. 辅助操作:设置、插件、导入导出、折叠、排序、二级管理入口。

每个主界面都应让用户快速回答:

- 我现在在哪个仓库 / 目录里?
- 当前资源是什么状态?
- 下一步可以对它做什么?

## 2. 布局系统

### 2.1 应用壳层

- 标题栏:当前位置、窗口级操作、全局状态。
- 左侧栏:仓库、目录树、快捷筛选、搜索入口、服务状态。
- 主工作区:当前页面内容。
- 资源浏览界面:文件列表是主要扫描区域,预览和元数据是主要判断 / 编辑区域。
- 管理页面:设置、插件、缓存、服务和相关配置。

### 2.2 信息密度

- 优先使用紧凑行、稳定控件和可预期的扫描路径。
- 避免落地页式构图、hero 区块、超大标题、装饰面板和营销式卡片流。
- 避免嵌套卡片。卡片只用于独立重复项、弹窗和工具容器。
- 侧栏行、图标按钮、chip、tab、文件列表行、预览容器和元数据字段需要保持稳定尺寸。

### 2.3 空间规则

- 壳层和侧栏元素应像桌面框架的一部分,不是漂浮装饰。
- 主内容应保留足够预览和阅读空间,但避免空洞的仪式化留白。
- 冲突、失败和危险操作应靠近相关资源出现,不要只藏在二级面板深处。

## 3. 色彩令牌

优先使用 `src/styles/` 中已有 CSS 变量和标准组件类。新增界面应先从这些令牌与 `.ui-*` 标准类派生,再考虑局部新增颜色或样式扩展。

### 3.1 表面

| 令牌 | 用途 |
| --- | --- |
| `--bg` | 主工作区表面。 |
| `--bg-elev` | 标题栏、侧栏、卡片、浮层、主要面板。 |
| `--bg-subtle` | 输入框、代码块、chip、非激活列表表面。 |
| `--bg-hover` | 行和轻量控件的 hover 背景。 |
| `--bg-active` | 激活行、选中 tab、按下状态。 |

### 3.2 边线

| 令牌 | 用途 |
| --- | --- |
| `--border-soft` | 低强调分隔线和内部边界。 |
| `--border` | 标准组件边界。 |
| `--border-strong` | 浮层、激活输入、强交互边界。 |

### 3.3 文本

| 令牌 | 用途 |
| --- | --- |
| `--text` | 主文本和可读内容。 |
| `--text-muted` | 次要标签、元信息、非激活控件。 |
| `--text-faint` | 占位、路径、弱提示、空状态。 |

### 3.4 强调色与语义色

| 令牌 | 用途 |
| --- | --- |
| `--accent` | 当前项、主操作、焦点、轻量激活状态。 |
| `--accent-strong` | 必要时使用的更强强调交互。 |
| `--accent-soft` | 选中 / 激活的柔和背景、轻量正向状态。 |
| `--ok` / `--ok-soft` | 成功、健康、同步完成。默认保持低强调。 |
| `--warn` / `--warn-soft` | 警告、冲突、部分配置、潜在风险。 |
| `--err` / `--err-soft` | 错误、危险操作、失败、删除确认。 |

### 3.5 色彩规则

- 用中性灰阶建立结构。
- 蓝色只用于当前、主操作和焦点,不做大面积装饰。
- 语义色必须表达状态或风险。
- 危险动作通常使用柔和红色背景加红色文本。
- 深浅主题都必须通过共享变量保持可读。

## 4. 排版

### 4.1 字体

- 界面文本:`--font-sans`。
- 路径、哈希、数据库字段、日志、代码:`--font-mono`。

### 4.2 字号

| 字号 | 用途 |
| --- | --- |
| 18px | 页面标题或管理页主标题。 |
| 14px | 默认正文和主要工作文本。 |
| 13px | 行标签、工具栏标签、紧凑标题、控件文本。 |
| 12px | 元信息、提示、徽章、次级说明。 |
| 10-11px | 数量、微型徽章、来源标签、低强调工具文本。 |

规则:

- 字号不随视口宽度缩放。
- 字距保持 `0`,除非已有组件确实需要其他设置。
- 中文界面文案保持短、直、可操作。按钮写动作,状态写事实。

## 5. 组件语言

### 5.1 按钮

- 熟悉工具优先使用图标按钮:刷新、同步、搜索、筛选、折叠、删除、重试、打开目录、窗口控制。
- 有可用图标时优先使用 `lucide-vue-next`。
- 需要明确语义的命令使用文字按钮。
- 按钮使用 `.ui-button` 作为基础,主操作加 `.ui-button--primary`,普通操作加 `.ui-button--ghost`,危险操作加 `.ui-button--danger`,纯图标按钮加 `.ui-icon-button`。
- 按钮不使用边框区分类型;普通按钮常态透明背景,主操作使用低饱和蓝色,危险动作使用低饱和红色。

### 5.2 列表与行

- 行需要支持 hover、active、muted 和 disabled 状态。
- 当前行使用激活背景加强调色文本。
- 次级行或子行可以使用弱化文本,但必须保持可读。
- 文件拖拽、目录放置、重命名和批量选择状态应提供即时但克制的反馈。

### 5.3 Chip、徽章与 Pill

- Chip 用于紧凑选项、标签、筛选器和下拉触发。
- Pill / 徽章用于可读状态和数量。
- 药丸形适用于徽章、chip、状态点和紧凑状态控件。

### 5.4 卡片与面板

- 卡片用于独立信息组、重复项、弹窗和被框定的工具。
- 不把页面 section 做成装饰性卡片。
- 不嵌套卡片,除非内部元素是真正的重复项。
- 页面 BEM 类只负责布局、区域命名和少量页面专属尺寸,不重新定义按钮、输入、tab、badge、列表项的基础外观。

### 5.5 输入

- 输入使用柔和背景、清晰边线和可见焦点。
- 搜索输入应贴近结果区域,减少视线跳转。
- 元数据输入应清楚区分字段名、字段值、状态和保存动作。

### 5.6 菜单与浮层

- 使用提升表面、强边线和 6-8px 圆角。
- 菜单应紧凑,并支持键盘可读。
- 危险菜单项使用错误文本和柔和红色的 pending / confirm 状态。

## 6. 资源工作台

资源工作台是核心视觉对象。它展示文件事实,也承载可维护的元数据。

### 6.1 仓库与目录

- 仓库身份应清晰可见,包括名称、路径、状态和最近同步信息。
- 目录树用于定位资源位置,不要和全局导航混淆。
- 当前目录和当前筛选条件应比辅助操作更突出。

### 6.2 文件列表

- 文件列表优先支持扫描、比较和批量操作。
- 列表项应稳定展示名称、路径、状态、扩展名、大小、修改时间和关键标签。
- 空状态应说明当前范围没有结果,不要写成营销式欢迎页。

### 6.3 预览与元数据

- 预览区展示资源内容或明确的不可预览占位状态。
- 元数据区展示标题、标签、自定义字段、插件生成信息和保存状态。
- 编辑态、未保存、冲突和保存失败必须靠近字段出现。

### 6.4 Revision 与事件

- Revision 和事件记录展示事实:什么时间、什么来源、做了什么、前后状态如何。
- 运行中状态可以显眼,完成后的过程信息应回落为低强调摘要。
- 冲突、失败和危险操作必须可定位到具体仓库、资源或字段。

## 7. 搜索、缓存与插件

### 7.1 搜索

- 搜索界面以条件和结果为主体。
- 筛选器应紧凑、可撤销、可组合。
- 结果项应突出匹配对象、所在仓库、路径和关键元数据。

### 7.2 缓存

- 缓存状态应表达容量、命中、失效和最近条目。
- 不把缓存指标做成装饰性仪表盘;优先帮助用户判断是否需要清理、刷新或调整配置。

### 7.3 插件

- 插件界面只展示能力、状态、来源、启用关系和风险。
- 插件卡片或列表项应优先支持比较和管理,不要做成产品推广卡。
- AI、OCR、向量检索等扩展能力必须清楚标记来源和处理状态。

## 8. 状态与反馈

### 8.1 状态层级

- 选中 / 当前:激活背景 + 强调色文本。
- Hover:轻量提升背景。
- Disabled:弱文本,不参与视觉竞争。
- Success:默认低强调,除非结果必须被注意。
- Warning:使用警告色并说明影响。
- Error / 危险:使用错误色并说明后果。

### 8.2 焦点

- 键盘焦点必须可见。
- 优先使用强调色边框或 outline,并确保深浅主题都有足够对比。

### 8.3 动效

- 动效用于表达连续性:侧栏折叠、弹窗出现、列表更新、扫描状态、树展开。
- 过渡保持短促、功能性。
- 动效不得干扰路径阅读、资源比较、元数据编辑或批量操作。

## 9. 文案

界面文案应像工程工具:直接、短、准确。

推荐模式:

- `正在扫描仓库`
- `已标记删除`
- `索引已更新`
- `同步中`
- `冲突`
- `未索引`

规则:

- 按钮写动作。
- 状态文本写事实。
- 提示写影响。
- 错误文本说明失败对象,并在可能时给出下一步。
- 避免营销口号、情绪化鼓励,以及在应用内解释视觉设计意图。

## 10. 评审清单

发布视觉或交互改动前,检查:

- 界面仍然像克制的资源仓库工作台。
- 主内容强于状态和辅助控件。
- 新颜色来自现有令牌。
- 控件优先使用 `.ui-*` 标准类作为基础。
- 深浅主题都可读。
- 文本不会溢出按钮、chip、行、卡片或面板。
- 控件在 hover、active、loading、empty 状态下尺寸稳定。
- 冲突、失败和危险操作靠近相关资源可见。
- 卡片没有嵌套,也没有被用作装饰性页面 section。
- 图标沿用现有图标语言。
- 如果引入新的交互模式,先确认它属于资源主线、当前状态、过程信息还是辅助操作。
